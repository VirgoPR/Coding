#include <iostream>
using namespace std;
void fact(int i, int sum){
    if(i<1){
        printf("%d", sum);
        return;
    }
    fact(i-1,sum+i);
}

int main() {
    int n;
    cin>>n;
	fact(n,0);
	return 0;
}
